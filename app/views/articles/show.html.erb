<h1><%= @article.title %></h1>

<p><%= @article.content %></p>

<%= link_to 'back to list', articles_path, class: 'btn btn-light' %>

<%= link_to 'edit this article', edit_article_path, class: 'btn btn-primary' %>

<%= link_to 'delete this article', article_path(@article), data: { turbo_method: 'DELETE', turbo_confirm: "Are you sure?" }, class: 'btn btn-danger' %>
