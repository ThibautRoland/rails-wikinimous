<ul>
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %>: <br>
    <%= article.content %> <br><br></li>
  <% end %>

  <%= link_to 'add a new article', new_article_path, class: 'btn btn-primary' %>
</ul>
